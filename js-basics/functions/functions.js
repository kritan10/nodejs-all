// Functions are reusable piece of code

// function example(args) {
//     // do somethign
//     return 0
// }

// a function contains
// - function name (optional for anonymous func)
// - parameters (optional)
// - return value (optional but default return is undefined)


// Regular Functions
// functions in JS can be declared using the function keyword
function addFive(num) {
    return num + 5
}

// Arrow functions
// functions in JS can be declared using the fat arrow syntax
const isOdd = (n) => { return n % 2 == 0 }