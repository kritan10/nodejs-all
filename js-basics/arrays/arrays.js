// arrays are collection of elements
// arrays in JavaScript can contain value of any type
// arrays can also have empty elements

const a = [1, "a", ["b"], () => { }, , , true]
console.log(a);

a.forEach((item) => { console.log(item); })