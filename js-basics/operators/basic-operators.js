// Assigment operator (=)
// The assignment operator is used to assign value to a variable
const a = 1;

// Compound assignment operator (+=, -=, *= /= ...)
// They are used to assign value after the corresponding evaluation
let b = 1
b += 5 // b = 6